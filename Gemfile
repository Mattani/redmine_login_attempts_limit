# Plugin Gemfile for redmine_login_attempts_limit (loaded by Redmine via eval_gemfile)
# Declare runtime/test gems required by the plugin when Redmine loads plugins' Gemfiles.

gem 'redis', '>= 4.0.1'
